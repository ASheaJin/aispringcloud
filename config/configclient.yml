server:
  port: 8070
eureka:
  client:
    service-url:
      # eureka.client.service-url.defaultZone ： 注册中⼼的访问地址。
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: configclient